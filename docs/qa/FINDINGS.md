# QA findings — bugs found by a QA phase, fixed by nobody yet

_Created 2026-09-15. Written by Phases 14–20. Read by whoever schedules the fix pass._

A QA phase **never fixes application code** (`DEVELOPMENT_GUIDE.md` §6). When it finds a
bug it does three things: writes the failing test, appends a row here, and keeps going.
Fixing is separate, scheduled work.

**Severity.** `HIGH` = money, permissions, or data loss. `MED` = wrong behaviour a user
would notice. `LOW` = cosmetic or copy.

| # | Phase | Severity | Where (`path:line`) | What is wrong | Failing test | Ticket |
|---|---|---|---|---|---|---|
| F-01 | 14 | ~~HIGH~~ **FIXED 14F** | `src/server/mis/payroll.ts:6-7` · `src/app/(mis)/mis/payroll/page.tsx:13` · `src/app/(mis)/mis/print/payslip/[employeeId]/page.tsx:22` · `src/server/mis/navigation.ts:51` | Payroll (basic pay, overtime pay, late penalty, gross pay) is computed and returned behind `attendance.read`, which ADMIN, SUPERVISOR, ATTENDANCE_OPERATOR and SUPER_ATTENDANCE_OPERATOR all hold. All four get the `/mis/payroll` screen, the printable payslip and a menu entry. Only `getMonthWageBill` (the owner-home card) is gated on `wages.read`. `getWageAmount` is deliberately ungated (`wage-type.ts:195`), so nothing inside stops it. D24. | `wage-leak.test.ts` "F-01 …" (4 roles) · `wage-screens.test.tsx` "F-01 …" (8) · `permission-matrix.test.ts` "F-01 …" (4) · `server-gates.test.ts` "calculateMonthlyPayroll is gated on wages.read" · `navigation.test.ts` "only a role that holds wages.read is offered the Payroll link" | MIS-46 |
| F-02 | 14 | ~~HIGH~~ **FIXED 14F** | `src/server/mis/business-rules.ts:208` | `getBusinessRules` (gate `settings.read`, so ADMIN) returns every non-AQL rule — including `DAILY_WAGE_DEFAULT`, `OT_MULTIPLIER`, `LATE_PENALTY_PER_MIN` if those rows exist in the live DB — to the Settings screen, whose props carry the values. Depends on those rows existing (nothing in this repo seeds them; the live DB may). | `wage-leak.test.ts` "F-02 …" · `wage-screens.test.tsx` "F-02 …" | MIS-46 |
| F-03 | 14 | ~~HIGH~~ **FIXED 14F** | `src/server/mis/business-rules.ts:219` | `updateBusinessRule` needs only `settings.write` (ADMIN) and accepts ANY key. An Admin can edit the wage rules (which move every payslip) and the four AQL thresholds, bypassing `aql.read` (D6) and `wages.read`. `updateAqlThreshold` guards its keys; this door does not. Reachable from the browser via `updateRuleAction`. | `wage-leak.test.ts` "F-03 …" (3 wage keys + 1 AQL key) | MIS-46 |
| F-04 | 14 | ~~MED~~ **FIXED 14F** | `src/server/mis/business-rules.ts:179-201` | Editing a wage rule writes `{ ruleKey, ruleValue }` — the old AND new wage — into the audit row. `redact()` matches property NAMES and `ruleValue` is not one. The audit screen does not display payloads, so this is at rest in the table, not on a page. | `wage-leak.test.ts` "F-04 …" · `audit-payloads.test.ts` (registry pins the one site) | MIS-46 |
| F-05 | 14 | LOW | `src/server/mis/audit.ts:9-31` | `redact()` is one level deep and knows seven names. Nested `{ row: { amount } }` and `basicWage`/`otPay`/`grossPay`/`latePenalty` pass through. Latent: every caller is clean today (`audit-payloads.test.ts`), so this is a net with holes, not a live leak. | `wage-leak.test.ts` "F-05 …" (2) | MIS-46 |
| F-06 | 14 | ~~MED~~ **FIXED 24C (BOM) + 24D (the rest)** | `src/server/mis/bom.ts:5` · `src/server/mis/reports.ts:98` | Material rates and stock prices are hidden by the screens (`isOwner &&`) but still sent: `getBom` returns every material's `ratePerUnit` to any `orders.read` role (ADMIN, SUPERVISOR, QC); `getStoreReport` returns `pricePerUnit` (and the raw transactions) to ADMIN, SUPERVISOR, QC, SUPER_ATTENDANCE_OPERATOR. PO rates (`MisPoItem.ratePerUnit`) and `MisItem.pricePerUnit` on the item screens were **not** checked — see the report. Policy is D24 (assumed). | `wage-leak.test.ts` "F-06 …" (`getStoreReport` ×4, `getBom` ×3) | MIS-46 |
| F-07 | 14 | LOW | `src/app/(mis)/mis/print/payslip/[employeeId]/page.tsx:22` | The payslip print page's only own check is `requireMisAccess` (the feature flag). Its role gate is whatever `calculateMonthlyPayroll` happens to demand — so F-01 opened it, and a future re-gating elsewhere changes who can print a payslip. It also opens any employee by id (F-11). | covered by the F-01 payslip cases | MIS-46 |
| F-08 | 14 | ~~HIGH~~ **FIXED 14F** | `src/server/mis/payroll.ts:9-19` · `src/server/mis/wage-type.ts:195` · `src/server/mis/business-rules.ts:229` | MIS-272 fails. Payroll is recomputed on demand and reads the wage rate, OT multiplier and late-penalty rate **as of now**, not as of the month being computed. Adding a rate effective today changes last month's payslip. The store is append-only and supports history; the readers do not use it. | `rule-history.test.ts` "F-08 …" (3) | MIS-272 |
| F-09 | 14 | MED | `src/server/mis/business-rules.ts:179` · `wage-type.ts:130` · prisma `@@unique([ruleKey, effectiveFrom])` | `effectiveFrom` is a `@db.Date` and part of the unique key, so a second edit of the same rule (or rate) on the same day collides — including the very first AQL edit, because the AQL defaults are created "today" when the screen is first opened. Proven against a fake that mirrors the constraint; **not run against Postgres**. | `rule-history.test.ts` "F-09 …" (3) | MIS-272 |
| F-10 | 14 | ~~HIGH~~ **FIXED 14F** | `src/server/mis/employee.ts:62,76` | Privilege escalation. `createEmployee` / `updateEmployee` take a `role` and need only `employees.write` (ADMIN). An Admin can set any row — their own included — to OWNER; `roles.ts` then resolves that login as Owner, granting `wages.read`, `aql.read`, `users.invite`. `assignableRoles('ADMIN')` omits OWNER but only the picker uses it. D25. | `employee-slice.test.ts` "F-10 …" (4) | MIS-43 |
| F-11 | 14 | MED | `src/server/mis/employee.ts:57` | D4 is enforced on the list but not on a lookup by id: any `employees.read` holder can open any employee — a Supervisor the other Supervisor's worker, an Admin the Owner. The payslip page is built on this. | `visibility-lists.test.ts` "F-11 …" (2) | MIS-49 |
| F-12 | 14 | LOW | `docs/DECISIONS.md` D19 · Phase 14 prompt | Two documents cited **D6** for the Owner-only wage rule. D6 is the AQL rule; the wage rule had no D-number (BRD S9 only). Fixed in the docs: D19 now cites D24, which records it. | — (documentation) | — |
| F-13 | 14 | MED | `src/server/mis/approvals.ts:4` | One call, one gate (`orders.read`), three kinds of data. QC receives every pending leave request (no `attendance.read`) and QC and SUPERVISOR every pending purchase order (no `po.read`). | `approvals-access.test.ts` "F-13 …" (3) | MIS-49 |
| F-14 | 24C | LOW | `src/server/mis/machines-board.ts` · `prisma` `MisMachine.isActive` | D5's "down" state is only `isActive = false`. Nothing records **when** a machine went down, **who** reported it, **why**, or when it is expected back, and no server function switches a machine back on — so D5's downtime panel says exactly that instead of showing a reason, and offers no "Mark back in service". Building either needs a downtime table (schema gate) and a `machines.write`-style permission decision. | — (behaviour asserted in `machine-timeline-desktop.test.tsx`: the panel offers no action) | Phase 24.4 |
| F-15 | 24D | LOW | `src/server/mis/bom.ts` `addBomMaterial` · `src/server/mis/po.ts` `addPOItem` / `updatePOItem` | **Writing** a price is not gated on `wages.read`, only reading it is. An Admin (`orders.write`, `po.write`) or Store Guy (`po.write`) can type a PO rate they can never read back, or overwrite the Owner's rate without seeing it; the BOM screen hides the rate box from non-Owners but the server action accepts one. Confidentiality holds (D24 is about what is *sent*); integrity and workflow do not follow from it. Decide first whether the person who raises a PO may price it — if so the rate box is a deliberate write-only field; if not, a rate from a caller without `wages.read` must be refused, and PO raising needs an Owner step. Not decided here (a policy choice) — put to Arjun with D24. | — | Phase 24.4 / D24 |
| F-16 | 24D | LOW | `prisma` `MisProductionLog` · `src/lib/mis/rule-keys.ts` | **D7 draws figures nothing records.** (1) **No wastage allowance** exists anywhere — no rule key, no per-machine or per-process column — so "against allowance, by machine", the 100% line, the red breach bar and the "vs allowed" column are not drawn; the machine card shows waste as a share of that machine's OWN output and says no allowance is recorded. (2) **No event log** — "ink change · 18 Aug" and "down 22% since…" cannot be derived; the chart says nothing is marked. (3) **One `unit` per log row** is shared by output and waste, so the artboard's "Kg of waste over Nos of output" is unrepresentable: the report shows one unit at a time and states its denominator in that unit. (4) **Machine utilisation** is not a report yet (the guide's own comment on `reports.ts` says so) so that tab is absent. Building any of these needs a table or a rule key — an allowance per machine/process is the one to put to Arjun. | — (asserted in `wastage-desktop.test.tsx`: no allowance line, no breach colour, no invented annotation) | Phase 24.4 |
| F-17 | 24D | LOW | `prisma` `MisAttendance` · `src/lib/mis/rule-keys.ts` · `attendance.ts` | **D8 draws figures nothing records.** (1) **No weekly-off or working-day rule** — "26 working days · 5 Sundays off", the Sunday tint and the "weekly off" cell state cannot be derived (`payroll.ts` counts every calendar day as a working day); the matrix uses every calendar day as a slot, marks none as off, and says so. A day with no row and no approved leave is **"not recorded"** (dashed), never absent. (2) **Approved leave writes no attendance row** — `approveLeave` only updates the request, and no code path writes `status: 'LEAVE'` on approval (only a manual `editAttendance` can); D8 unions approved requests onto the grid, so leave shows, but the phone `getDayAttendanceSummary` (which counts `LEAVE` rows only) undercounts it. (3) **Overtime is stored separately from the punches** (D20) and the artboard's own example rounds 11 h 58 min to "12.0 h" before subtracting; D8 shows the recorded figure beside what the two punches imply and says when they differ. (4) **The "hourly rate — monthly ÷ 26 ÷ 9" line** is omitted: it is pay, not attendance (D24), and D26 replaced that formula with a per-hour rate on the wage code. (5) **"Payroll export"** — no export exists; the Owner gets an "Open payroll" link. (6) **Pool scoping** — D8 is not narrowed by D4's people scope, exactly like the day register `listAttendance` (the open decision recorded there): the person running payroll manages nobody. "Pool" is a department filter. | — (asserted in `attendance-month-desktop.test.tsx` and `attendance-month.test.ts`) | Phase 24.4 / 25 |
| F-18 | 24D | LOW | `src/server/mis/qc.ts:191` · `prisma` `MisQcCheck` · `src/lib/mis/order-timeline.ts` (`hourlyQcStrip`) | **D9 draws figures nothing records, and found two older inconsistencies.** (1) **A QC check has no machine** — it belongs to an ORDER — so D9's rows are orders, with the machine taken from the booking that overlaps the shift (absent when none); the artboard's "line" filter is not drawn. (2) **The phone `getTodayQcBoard` and the capture grid page fix the hours at 09:00–16:00 and read them with the server's clock** (`setHours`/`getHours`) — a D22 site for the Phase 20 sweep, and against D9's own "nine columns because the shift is nine hours"; D9 reads the shift setting and the factory zone instead and does not reuse it. (3) **D4's `hourlyQcStrip` counts every non-FAIL check (including N/A make-ready) as PASS**, so the order screen's strip shows a make-ready hour green where D9 shows it grey; not changed here (D4 was already PASS) — fold into the next QC pass. (4) **"Failed · defect raised"** — a failed check does not record whether a defect was raised (D14's defect log is where that would live). (5) **AQL "of 40,000" and "thresholds effective 01/07/2026"** — orders carry no quantity (D13) and the decision's audit snapshot stores the limits but not their effective date; the panel shows the sample and the limits as scored. (6) **AQL limits are `aql.read` (D6):** the panel's `limits` key is absent for ADMIN, SUPERVISOR and QC — they see the verdict and the sample only. (7) **When a line was running comes only from its machine BOOKING** — an order has no start-of-production time. So hours outside a booking are drawn "not running" (nothing was due), lines are the orders booked in the shift plus anything checked plus, for the current shift only, orders in production; an order in production that has NEVER been booked has an unknown run window and every hour is treated as due (the row shows no machine); one booked only on another shift, or whose booking was released, has nothing due in this one. (8) **"QC report"** — no such page exists; the primary action is "Record a check" for a role that may write. | — (asserted in `qc-grid.test.ts` and `qc-grid-desktop.test.tsx`) | Phase 24.4 / 20 |
| F-19 | 24D | MED | `prisma` `MisQcCheck` · `src/components/mis/qc/qc-detail-screen.tsx:226` · `src/server/mis/qc.ts` `recordAqlSample` | **D14 draws figures nothing records — the largest gap of the four screens.** There is **no defect log**: a defect is a `FAIL` `MisQcCheck` with a **free-text** `defectType` (the capture screen has a plain input, not the defect-type master), a `defectQty` with **no unit**, and no batch, disposition or cost. The AQL path DOES use master ids, but persists only totals on the check and the per-defect lines only inside the `AQL_DECISION` audit snapshot. So D14 is built from FAIL checks and: (1) **severity is only the master's** — text matching a master by code/name is classified, everything else says "not classified" (never guessed, never typed by the person); the artboard's "Severity comes from the master" is therefore only partly true until capture uses the master. (2) **No disposition** (reworked / scrapped / accepted with deviation), **rework time**, **batch**, or **"cost of poor quality"** (money — Owner-only, D24 — with no source and no rate on the check): a single dashed "Not recorded yet" card says so; none is computed and none is in the payload. (3) **No defect RATE or tolerance** — the denominator ("412,500 sheets produced") needs output in the SAME unit as the defect. `MisProductionLog` DOES record machine, shift, `qtyProduced` and a `unit` (default KG) per run, and `MisMachine.capacityPerDay` exists — but a defect quantity has no unit at all, so no run record can be matched to it honestly. Machines are ranked by quantity and the screen says the artboard's own warning applies (count picks the busiest machine). **The fix is a unit on the defect quantity**, then a rate per machine from the run records. (4) **The machine is derived** from the booking covering the moment of the check (one → that machine, several → "several", none → "no booking"); shift from the factory clock against the shift settings. (5) **"Export"** is not built. (6) **AQL defect lines** (with proper master ids and severities) are not in the log — they live only in the audit snapshot. Fixing (1)/(6) means capture writes a defect row per master id — the schema decision to put to Arjun with D14's disposition question. | — (asserted in `defects.test.ts`, `defects-desktop.test.tsx`) | Phase 24.4 / 20 |
| F-20 | 24E | LOW | `prisma` `MisMachine` · `MisItem` · `src/server/mis/{machine,process,item}.ts` · `src/components/mis/kit/data-table.tsx` | **D10 draws things nothing records, and found two older inconsistencies.** (1) **A machine and a raw material have no Hindi name** (`MisMachine` and `MisItem` have no `nameHi`), so D10 draws no Hindi column for them — "not set" would be a lie for a name that can never be set. Adding the column is a **schema change** (SCHEMA GATE, Half A: schema + SQL in `migrations-pending/`, then stop) — not done here. (2) **Checklists, Pools and Paper types have no master**; the sub-nav says so instead of showing dead entries (Pools would be D4's manager tree, paper types are at best the `SUBSTRATE` option group). (3) The artboard's machine **Running / Free / Down** badge is derived from the board (D5), not master data; **"Edited 05/09 · A. Bhaskar"** has no source on a master row (the editor is only in the audit log). (4) **Code is read-only after creation in D10** (the form does not submit it and `saveMaster` never passes it on), but the phone layer's `updateMachine` / `updateDept` / `updateProcess` / `updateItem` / `updateCustomer` still ACCEPT a code — a save from the phone screens can still change it; enforcing it in those server functions would change the phone screens too. (5) **Customers are gated on `orders.read` / `orders.write`, not `masters.*`** — the D10 list gates on `masters.read` first, then delegates. (6) **`DataTable` (phone layer) switches table/cards at `md` (768px)** — a second breakpoint against D3's "exactly two layouts"; D10 uses only `lg`, and the old screens are left as they were. | — (asserted in `master-directory.test.ts`, `master-data-desktop.test.tsx`) | Phase 24.4 / 20 |
| F-21 | 24E | MED | `src/server/mis/store.ts` `commitIssue` · `prisma` `MisInventoryLedger` · `MisGrnItem` | **D11 is honest about a chain the records cannot form.** (1) **The store does not record which LOT an issue drew from**: `MisGrnItem.batchNo` records a receipt's lot, and `MisInventoryLedger` (`source: 'STORE_ISSUE'`, `sourceId` = the order) records what was issued to an order — but stock is fungible per item and nothing links an issue to a receipt. So "supplier lot → batch → order" cannot be traced; D11 shows the receipts of an order's BOM items (item level) and, for a lot, the orders issued the same ITEM afterwards, labelled "may or may not have used it". The artboard's "1,240 + 640 + 520 = 2,400 — accounted for" needs a lot-level issue record and is not drawn. (2) **No despatch record**: an order has a status, but no despatch with a person and a time — step 6 is always a drawn gap, and the green "Contained" verdict is NOT built (asserting it would be a claim the data cannot support). (3) **No recall notice** exists (no model), so the "Supplier recall notice" chip, "Trace forward" and "Export chain" are not built. (4) **Who** issued/received comes from `createdById` / `receivedById` (user-profile ids); an id with no profile shows "person not recorded", never another name. (5) A GRN item's `forOrderRef` (free text) is not used — an unstructured reference is not a link. Fixing (1) needs an issue → receipt link (schema; SCHEMA GATE Half A) — a decision for Arjun with D14's disposition question. | — (asserted in `trace.test.ts`, `traceability-view.test.ts`, `traceability-desktop.test.tsx`) | Phase 24.4 / 25 |
| F-22 | 24E | MED | `src/server/mis/business-rules.ts` `getRuleValue` · `getAqlThresholds` · `getLineClearanceRule` · `getOfflineRules` · `getFactoryTimezone` · `getCorrectionWindowDays` | **D12 draws a claim the code does not keep, and some things nothing records.** (1) **Most rule readers read the value in force NOW, not "as of a date".** `getRuleValue` takes no date; there is no `getSettingAsOf`. The artboard's "every screen that uses a rule asks for it as of a date — a COA printed for a July lot is judged by July's ceiling" is true today only for payroll's wage history (F-08) and for QC decisions already stored; AQL thresholds, line clearance, offline tolerances, the timezone and the correction window resolve to the current row. A scheduled change therefore takes effect on its start day (correct going forward), but a reprint or re-judgement of PAST work would use today's value. The screen says so ("others still use the value in force now") and shows any rule as of any day itself. **Fix (Phase 25): an as-of reader and a date argument on the callers that judge past records.** (2) **Units are not recorded on a rule** (`%`, `min`, `days` in the artboard) — none is drawn; a `unit` column is a schema change (SCHEMA GATE, Half A). (3) **The artboard's keys (`qc.aql.major.max`, `shift.1.window`, `waste.allow.printing.pct`, `payroll.hours.per.day`) mostly do not exist**: real keys are `AQL_MAJOR_MAX`, `OT_MULTIPLIER`, `factory.timezone`…; shift windows and wastage allowances are not rules; and a rule appears only once it has a row (several are created on first use). (4) **A reason has no column** — it lives in the `SCHEDULE_RULE` audit row; rows written by the older paths (`updateBusinessRule`, the AQL and wage screens) have none and show none. (5) **The older `/mis/settings` list still lets an Admin change a general rule effective NOW with no reason** (D30). (6) The artboard's per-row ⋮ menu is not built: with no edit or delete, a row has nothing to offer. (7) The seeded row is labelled "seeded default", not "migration" — nothing records that a migration wrote it. (8) **`getRuleValue` compares a row's start against `new Date()` (UTC); the screen says a row is in force from the FACTORY's midnight** (D22). A change scheduled for an IST day starts at 00:00 UTC of that date, i.e. 05:30 IST, so for about five and a half hours the ledger says "in force" while the readers still use the old value. Fix with the as-of reader in (1). (9) **The audit write never throws by design** (`logAuditEvent`), and the row and its audit entry are two writes, not one transaction — a failed audit write leaves a rule row with no reason on record. Project-wide; a transaction would need the audit writer to accept one. (10) The artboard's **"Owner · A. Bhaskar"** actor line and the **"· today"** created-on stamp on the scheduled row are not drawn (the actor is the signed-in Owner; `updatedAt` exists but the ledger does not carry it). (11) **The server's refusal messages are English only** (`validateSchedule` / `validateRuleValue`), so a Hindi-locale Owner reads an English reason for a refusal; the labels and hints are translated. (12) A row's per-key validity is a table in `lib/mis/rules-ledger.ts` (whole-count keys, the line-clearance mode) — a new rule key with a special format is not validated until it is added there; the older `updateBusinessRule` list does not apply it at all. | — (asserted in `rules-ledger.test.ts`, `rules-desktop.test.tsx`, `business-rules-schedule.test.ts`) | Phase 24.4 / 25 |
| F-23 | 24E | MED | `prisma` `MisDocument` · `src/server/mis/documents.ts` · `src/components/mis/documents/documents-screen.tsx` · `src/app/api/mis/docs/[orderId]/route.ts` | **D13 draws a document store that does not exist, and found one live hole.** A `MisDocument` is a **name + a pasted link** (`filePath`) to a file the MIS does not hold, attached to one order, with an optional size, type and description; "File upload will be added in a future version" (the phone form says so). So: (1) **No Generated / Uploaded split** — a generated document (job card, COA, QC report) is a print page made from an order, never a stored row; there is no category, so "Job cards 96 · Certificates 41 · Customer POs 34…" and the "IRREPLACEABLE" tag are not drawn. The groups are by recorded file type. (2) **No versions, no "superseded by", no retention** ("V1", "kept 7 years") — no column; each reads "not recorded". (3) **No batch on a document** — search is by order, name, description or link, not "batch"; the artboard's "ORD · B-2280" has no source. (4) **No preview** — the file is not held and a pasted URL must not be framed; the pane says so. (5) **No upload, no per-file progress, no queue** — the desktop add takes a link (with the typed values kept on failure). (6) **"1.9 GB of the bucket"** is not knowable: sizes are optional, so the header says "N of recorded size (M without a size)". (7) **"Unlinked · 0"** is not drawn as a count: `orderId` is required and the order cascades, so it is structurally zero — said in words. (8) **The phone screen turns any pasted string into an `href`** (`<a href={r.filePath}>`), and `addDocument` accepts any string — a stored `javascript:` link runs when the next person clicks it. The desktop never links an unsafe link and its add refuses one (D31); **the phone screen and `addDocument` are unchanged** (phone-screen edit = a PR line), so this is open. (9) `addDocument` audits `after: rec`, which includes `filePath` — a signed URL's token would land in the audit log, which the Admin can read. (10) **`/api/mis/docs/[orderId]` is `orders.read`** (unchanged) and the desktop read is the same gate plus the D4/D5 order seam; a document is tied to an order, so D5 pool scoping does not apply to it and the uploader is shown by display name only. (11) **No "Download" button** — only "Open": a pasted cross-origin URL cannot be forced to download, so the artboard's second button is not drawn. (12) **The add form lists the newest 300 orders**; an older order cannot be attached from the desktop (the phone screen lists all). (13) **Refusal messages are English only** (`validateDocumentInput`, "That order was not found."), so a Hindi-locale person reads an English reason; labels and hints are translated. (14) The file-type chip is neutral, not the artboard's amber, which it reserves for IRREPLACEABLE. **The fix for (1)–(6) is a schema decision** (categories, version, retention, batch, an upload path) for Arjun — SCHEMA GATE, Half A, not done. | — (asserted in `document-library.test.ts`, `documents-desktop.test.tsx`) | Phase 24.4 / 25 |
| F-24 | 24F | HIGH | `src/server/mis/store.ts` `listStoreItems` · `listStockSummary` · `getStoreDashboard` · `src/server/mis/inventory.ts` `getInventorySummary` · `src/app/(mis)/mis/{grn,bom,po,masters/items,reports}/…/page.tsx` | **Store, Inventory and GRN did not open — three separate causes, all found in the browser (WALKTHROUGH-24F.md).** (1) **One query per item on a one-connection pool.** The runtime pool is `max: 1` on purpose (`createPoolConfig`); every whole-store page asked for each item's latest balance separately (176 queries on the live data), the queue outlasted the 10 s connect timeout, and `/mis/store`, `/store/{stock,dashboard,count,transactions,ledger}`, `/mis/inventory` and the Store Manager's own home died with "timeout exceeded when trying to connect". Now ONE query per page (`SELECT DISTINCT ON (item_id) … ORDER BY item_id, created_at DESC` in `store.ts` and `inventory.ts` — read-only tagged templates with no interpolated input; `listPickerItems` and the healthz route already use `$queryRaw`). It sorts the whole ledger per page: there are only single-column indexes on `item_id` and `created_at`, so a composite `(item_id, created_at DESC)` index is worth adding in a later schema phase (SCHEMA GATE, Half A) — fine at today's size, linear growth after. **Any new whole-list page that runs a query per row will do the same; the pool width is not the bug.** (2) **A Prisma `Decimal` reached a Client Component** (§2A.5): `/mis/grn/[id]` crashed (`v.toNumber is not a function`), and BOM, PO, item master and reports logged "Decimal objects are not supported" (quantities could arrive as empty objects). The pages now pass their rows through `lib/mis/plain.ts` `toPlain` (Decimal → number; the money helpers still run first, so no price is added). (3) **`/mis/grn` was refused for a Supervisor** (`grn.read`, no `po.read`) because the page also fetched purchase orders for the "New GRN" form; it now asks only when the caller may write. **Not changed:** the write paths still read one balance at a time (single-item, fine). **One existing test edited on purpose:** `store-money.test.ts`'s fake database gained `$queryRaw` (its assertions are unchanged) because the code now asks for balances that way. | `store-pool.test.ts` (fails on the old code: 9 of 10 cases), `client-props.test.tsx` (fails per page without `toPlain`), `grn/page.test.tsx` (all 8 roles) | Phase 25 / 20 / 21 |
| F-25 | 24F | ~~MED~~ **FIXED 24G** | `src/app/(mis)/mis/**/[id]/page.tsx` (19 of 19 pages) · `src/lib/mis/ids.ts` | **A bad id in the address bar crashed the page.** `/mis/production/sign-off/none` showed "Something went wrong" (`invalid input syntax for type uuid`) instead of "not found", on every `[id]` route. Each page now answers 404 through `isUuid` before any query; a test reads the source of every dynamic page so a new one cannot skip it. The two `[orderId]` API routes (`/api/mis/docs`, `/api/mis/trace`) did the same, and worse: they compared the error message to `'Forbidden'`, which a `MisForbiddenError` ("Not permitted: …") never equals, so a refused role got a 500 (docs) or a 404 (trace) instead of a 403 — fixed with the same guard and `isMisForbiddenError` (`id-routes.test.ts`). **24G closed the last one:** `print/payslip/[employeeId]` is now guarded too; `wage-screens.test.tsx`'s `'e1'` fixture became a UUID (`wage-world.ts`), and `dynamic-route-guards.test.ts` lost its exception. | `dynamic-route-guards.test.ts`, `ids.test.ts`, `api/mis/id-routes.test.ts`, `print/payslip/payslip-id-guard.test.ts` | — |
| F-26 | 24F | MED | `src/components/mis/kit/data-table.tsx` · 20 list screens · `lib/mis/factory-time.ts` | **Screens that "render wrong".** Fixed: (1) the search box on 20 list screens was 14 px / ~38 px on no background with a faint border, so the placeholder was almost invisible (the MasterTable sheet draws 48 px / 16 px, white); they and 12 filter dropdowns/dates on the store and attendance screens now follow the rule, and a test reads every screen's source. (2) `/mis/employees` was 275 px wider than a 1440 window: `DataTable`'s desktop table now scrolls inside its own box, long e-mail addresses wrap, and the screen is one step wider. (3) A hydration mismatch on `/mis/settings/wages` and the clearance history (`toLocaleDateString()` with no zone): `formatFactoryDate` / `formatFactoryDateTime` write one string in the factory's zone. (4) The D10–D13 search fields and the D12 "As of" date had no text colour (faint grey); a test now requires one on every desktop field. **Still open:** (a) **tap targets under 44 px** — 295 at 390px on Store Stock (the row links and small buttons), 16 on the GRN list ("View"), 33 on the machines master at 1440 — **24G swept every screen against the measured list and raised the kit `Button`/`LangPill` defaults plus ~60 named call sites** (see `qa/UI-GAPS-24G.md`); treat as narrowed, not closed — no re-measure was run after 24G; (b) **about 60 more `toLocaleDateString()` / `toLocaleTimeString()` calls (plus 43 `toLocaleString()`)** without a zone (they print in the server's zone, not the plant's — D22); the client-side default zone is `DEFAULT_FACTORY_TIMEZONE`, not the stored rule — 24G re-confirmed these (`UI-GAPS-24G.md` G3-18), still open; (c) `DataTable` swaps to cards at `md` (768 px), a second breakpoint against D3 (also F-20 (6)); (d) other raw form controls on phone screens outside the ones above. | `data-table.test.tsx`, `search-inputs.test.ts`, `factory-time.test.ts` | Phase 24.4 / 25 |
| F-27 | 24F | ~~MED~~ **FIXED 24G, D32** | `src/components/mis/home/bottom-nav.tsx` · `src/lib/mis/phone-more.ts` (new) · `src/components/mis/home/more-sheet.tsx` (new) · `src/server/mis/navigation.ts` | **On a phone, Store, Inventory, GRN, PO, Suppliers, BOM, Traceability, Documents and Customers could not be reached by tapping** for Owner, Admin or Supervisor. D32 (ASSUMED): for these three roles the fifth tab is now "More", a bottom sheet listing every screen the role may open, derived from the same server-side permission table (`navigationForRole`) the desktop sidebar reads, so the two cannot disagree. The tab each role's More displaced (Owner: Settings; Admin: Reports; Supervisor: Me) is reachable inside the sheet. Other roles' five tabs are unchanged. | `navigation-more.test.ts` (all 8 roles), `more-tab.test.tsx` | — |
| F-28 | 24F | LOW | `src/app/(mis)/mis/error.tsx` (the boundary) · every page that calls `requirePermission` | **A refusal is thrown and caught by the error boundary**, so every "You do not have access" page writes 6 console errors (`MisForbiddenError`) even though the message shown is right; harmless to the person, noisy for anyone reading a console or an error tracker (a real error is hard to spot among them). | — | Phase 26 |
| F-29 | 24G | LOW | `src/server/notifications/` (server module only) | **The notification inbox (W1) has no route or screen at all** — only a server module exists, no page under `src/app/(mis)/mis/`, no nav entry, no bottom-nav tab. A full feature build, not a polish fix; left untouched by 24G. | — | unscheduled |
| F-30 | 24G | LOW | `src/app/(mis)/mis/production/[id]/page.tsx` · `src/components/mis/production/production-detail-screen.tsx` | **`/mis/production/[id]` does not implement P4's two-tap production-entry pattern.** P4 specifies: tap the running phase card → a dedicated numeric-keypad screen (60 px number keys, one required field, Save). The shipped route is an order-scoped log-plus-table screen instead — a different, reasonable design, but not P4's. Structural; not a layout fix. | — | unscheduled |
| F-31 | 24G | LOW | `src/app/(mis)/mis/machine-board/[id]/page.tsx` · `src/components/mis/machines/machine-detail-screen.tsx` | **`/mis/machine-board/[id]` has no D5 day-timeline at any width.** D5 specifies a Gantt-style per-machine timeline (bars across shift hours, a blocked-jobs side panel, a weekly utilisation chart, a "free right now" list); the shipped screen is a plain status-plus-history list. 24G fixed the colour convention within the existing template (amber not red for "busy") but did not build D5. | — | unscheduled |
| F-32 | 24G | LOW | `src/app/(mis)/mis/approvals/page.tsx` · `src/components/mis/approvals/approvals-screen.tsx` | **`/mis/approvals` does not match P5's grouped-by-order queue.** P5 shows one card per order (BOM/PO together), an age-coloured badge and a "changed since v1" delta, with Approve + Open on the row; the shipped screen groups by approval TYPE with no diff and no age colour. Related to the still-open 14F question ("Approvals is an Owner surface in the design… `getPendingApprovals` serves every `orders.read` role") but distinct — this finding is the visual/structural layout, not the permission question. | — | unscheduled |
| F-33 | 24G | LOW | `src/app/(mis)/mis/payroll/page.tsx` · `src/components/mis/payroll/payroll-screen.tsx` | **`/mis/payroll` does not match its own design (W9) and has no phone card view.** W9 is a month-end payroll-EXPORT flow; the shipped screen is a per-employee ledger with totals, CSV and a payslip link per row — a different, reasonable screen, but not W9. It also renders a fixed `<table>` at every width instead of the kit `DataTable`'s card layout below `lg`, unlike every other list screen. | — | unscheduled |

**Phase 24C (2026-09-22) fixed the BOM half of F-06:** `getBom` now removes `ratePerUnit` from every material for a role without `wages.read` (ADMIN, SUPERVISOR and QC get the structure without the rate; the four roles without `orders.read` are refused), and the three `it.fails` BOM markers became ordinary tests in `wage-leak.test.ts`. The D6 check then found a second route to the same rate — `getOrderTrace` (`/api/mis/trace/[orderId]`, `orders.read`) ran its own `misBom` query — now closed through the shared `withoutRates` and tested for all eight roles (`traceability-money.test.ts`). **Still open in F-06:** `getStoreReport` (`pricePerUnit`, raw transactions), PO item rates, `MisItem.pricePerUnit`, and the BOM **write** path (`ADD_BOM_MATERIAL` audits `after: rec`, which carries the rate — audit payloads must never hold money). Also new: **F-14** (machine down/up has no recorded facts and no way back). D6's costing is a new server function, `getBomCosting`, `wages.read`, refused for all seven non-Owner roles.

**Phase 24D (2026-09-22) closed the rest of F-06** — before D7 Reports, which builds on `getStoreReport`. Every route to a material price now goes through one helper, `lib/mis/money-fields.ts` (`withoutMoneyFields`: removes `ratePerUnit` / `pricePerUnit` at any depth) unless the caller holds `wages.read`: `getStoreReport` (not selected AND stripped from `raw`), `getPO`, `getGRN` (a GRN carries its PO's lines — found by the sweep), `listItems` / `getItem` / `searchItems` (Admin, Supervisor, QC; the masters and PO screens pass the whole list to the browser). `computePoTotal` IS money, so it is now `wages.read` (7 roles refused, zero queries); the PO detail, supplier and print pages ask only for the Owner, and a printed PO says prices are the Owner's. **The audit payload:** the audit writer's redaction list now includes the same two keys, so `addBomMaterial`, `addPOItem`, `updatePOItem` and the store item update store `[redacted]` — the Owner loses the old/new price history in the audit trail, the same trade D24 made for wages. The four `getStoreReport` `it.fails` markers became ordinary per-role tests (`wage-leak.test.ts`); `material-money.test.ts` holds the PO / GRN / item / audit cases, all eight roles each. Eleven mutations (helper, gates, select, audit list) each turned named cases red. **The checker's second pass** found two more: the WRITE functions (`addPOItem`, `updatePOItem`, `removePOItem`, `createItem`, `updateItem`, `deleteItem`, `restoreItem`, `addBomMaterial`) returned the whole row, so an Admin calling `updateItem(id, {})` would read a stock price — they now return through `forRole` (Owner: the row; everyone else: the row without its price), and `store.ts`'s `actor.role === 'OWNER'` gates on `listStoreItems`, `listStockSummary` and `getStoreDashboard` finally have tests (`store-money.test.ts`, all eight roles; loosening any one turns cases red). Still not a leak, but noted: `inventory/[itemId]/page.tsx` reads `MisItem` straight from the database and forwards only name, code and unit.

**Phase 14F (2026-09-21) fixed F-01, F-02, F-03, F-08 and F-10 — and F-04, in the same function as F-03.** Each `it.fails` marker was removed and replaced by ordinary passing tests, including one per non-Owner role asserting the SERVER FUNCTION refuses (`phase-reports/phase-14F.md`). The rows below keep their original description as the record of what was wrong. **Still open:** F-05, F-06, F-07 (partly — the page now has its own gate, the by-id lookup is F-11), F-09, F-11, F-13, and the questions section. **Not done:** the guide's "a computed period is immutable once closed" (F-08's second half) — it needs a stored payroll snapshot, recorded in D27 and left for Phase 25.

**How to read the `it.fails` markers.** Every finding above is asserted as the CORRECT behaviour and wrapped in Vitest's `it.fails`: the suite is green while the bug exists and a case turns **red the day the bug is fixed** — that is the cue to delete the `.fails`, and the case becomes a permanent guard. 17 cases remain after 14F (F-05 ×2, F-06 ×7, F-09 ×3, F-11 ×2, F-13 ×3); the 32 for the fixed findings became ordinary tests. Each was also run un-inverted to confirm it fails for the intended reason, not because of a fixture slip; and re-gating `calculateMonthlyPayroll` on `wages.read` in a scratch copy flipped 17 of F-01's 18 cases red (the 18th is the menu entry, a separate fix).

### Found while fixing (14F)

- **Audit rows written before 14F may already hold wage values.** Every `UPDATE_RULE` row for `DAILY_WAGE_DEFAULT`, `OT_MULTIPLIER` or `LATE_PENALTY_PER_MIN` carries the old and new figure in `before`/`after`. The code no longer writes them; the existing rows were not touched or read. Whether to scrub them is a data decision for a person with database access.
- **`updateEmployee` reads a row and then writes it outside a transaction**, so an Admin restating `OWNER` on a row an Owner has just demoted could undo the demotion in that gap. Narrow; not fixed.
- **D25's second half is still open:** an Admin can demote or deactivate the Owner's own record.
- **`effectiveFrom` defaults to the server clock** (`new Date()`) in `addWageRate` and `createRuleRevision`, so "today" is the UTC date, not the factory's (D22). Phase 20 sweep.

### Not bugs — questions for Arjun (no test asserts an answer)

- **Owner lock-out (D25, second half).** An Admin can `deleteEmployee` or re-role the Owner's own record and the Owner then resolves to no role. Nothing stops it; whether it should is unanswered.
- **AQL numbers seen by QC and Admin.** `recordAqlSample` (`qc.ts:97`) returns and audits the thresholds it scored against, so a QC user sees the limits while D6 is about who may *edit* them. D6 does not say whether *seeing* them is Owner-only.
- **Approvals is an Owner surface in the design** (`MIS_UI_SPEC` §4.5 gives the Owner the Approvals tab; D4's evidence says Admin has none) but `getPendingApprovals` serves every `orders.read` role. Only F-13's two kinds are asserted.

### Sites that bucket by server time (Phase 20)

No test in this phase tripped over one — but note that the fake database used by the payroll tests **ignores the date filter**, so `calculateMonthlyPayroll`'s server-local month bounds (`payroll.ts:24-25`, already on the Phase 20 list) are not exercised. Whoever fixes F-08 should test the month boundary at the same time.


<!-- APPEND NEW ROWS DIRECTLY ABOVE THIS LINE. -->

---

_Guide: [`../DEVELOPMENT_GUIDE.md`](../DEVELOPMENT_GUIDE.md) §6_
