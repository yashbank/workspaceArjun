# Phase 24 · Desktop layer (D1–D14)

**Date:** 2026-09-21 (Part A), 2026-09-22 (Parts B, C, D, E)   **Model:** opus (A, B), sonnet (C, D, E)   **Result:** PARTIAL — 24.1–24.3 done; **24.4 complete: every desktop screen D4–D14 is built** (24E added D10 master data, D11 traceability, D12 business rules, D13 documents). Still open: three dashboard widgets show empty states, no real-browser pass, decisions for Arjun.
**Tickets:** Phase 24 (E7-13, E1-04). No Jira ticket exists for this phase.

## Part E (2026-09-22) — D10 master data, D11 traceability, D12 business rules, D13 documents

Each screen: build agent → mutation test (a copy of the file is mutated, the related tests run, the file restored) → mis-fe-check + mis-be-check (sonnet) → fix → re-check. **The next screen did not start until the current one passed.** All four PASS (FE and BE).

### The four screens
- **D10 · `/mis/masters/*`, `/mis/customers`** — the desktop half of the master pages, **reusing** the phone layer's `listX` / `createX` / `updateX` / `deleteX` / `restoreX` through one adapter (`server/mis/master-directory.ts`: `getMasterDirectory`, `getMasterList`, `saveMaster`, `setMasterDeactivated`, all gated on `masters.read` / `masters.write`). Docked edit panel beside the list; **a code is read-only after creation** (the form never submits it and `saveMaster` drops it). A failed save keeps what was typed. Deactivate is idempotent. Honest gaps: **F-20** (no Hindi name on machines or items, no Checklists / Pools / Paper-type masters, no Running/Free badge, no "edited by" on a row, phone `updateX` still accepts a code).
- **D11 · `/mis/traceability`** — one search (order number, or supplier lot); `getTraceView` composes the phone reads (`getPhasesForOrder`, `getQcForOrder`) and the GRN / ledger tables. **Per-step permissions** (`grn.read`, `store.read`, `phase.read`, `qc.read`) — a step the role may not read says so, a step with no record is a **dashed gap**. Orders are looked up through `resolveVisibleOrderWhere` (D4/D5). **No "Contained" verdict** — the store records no lot on an issue, an order has no despatch record and there is no recall notice: **F-21**.
- **D12 · `/mis/settings/rules`** — **Owner only and absent (404) for the other seven roles** (`wages.read` is the Owner marker, D24/D25). List of rules with the value in force on a chosen day, a **"Schedule change"** form (new value, start day, **reason required**), and a history per rule (Scheduled / In force / superseded, ranges tiling exactly, a seeded row says "seeded default"). `scheduleBusinessRule` writes a NEW row and edits nothing; the start day is the **factory's** (D22) and not in the past; a same-day row is refused in words (and a P2002 race is mapped to the same words); **every change writes a `SCHEDULE_RULE` audit row carrying the day and the reason, and a wage rule's audit names the key, never a figure (F-04)**. Per-key validation stops a value the reader would silently ignore being shown as "in force" (`0` correction days reads as 3; `"shift"` as JOB; `0x10`, `1e3`). The audit "before" is the row the new one REPLACES, not the newest row on file. Honest gaps: **F-22** (the artboard's "every screen reads a rule as of a date" is false — `getRuleValue` takes no date; no units; keys differ; UTC-vs-factory-day lag; audit write is best-effort; English-only refusals). **D30** filed (who schedules, is a reason required).
- **D13 · `/mis/documents`** — a library across orders: type groups with counts, a factory-month filter, search by order / name / description / link, a paged newest-first list and a detail pane; an **Add document** form for roles that hold `orders.write` (absent, not disabled, for the rest). `getDocumentLibrary` (`orders.read`) and `saveDocumentLink` (`orders.write`, reusing `addDocument` so the audit row is the same) — every documents query is composed with `resolveVisibleOrderWhere` (D5: only people narrow by pool, so the seam is `{}` today; a document is tied to an order, and only the uploader's display name is a person). **A pasted link is followed only if it is http(s) or a site path** — the desktop never turns anything else into an `href`, and its add refuses one on the server. Honest gaps: **F-23** (a document is a name + a link — no Generated/Uploaded split, versions, retention, batch, preview or upload; "Unlinked 0" and "GB of the bucket" are not drawn; **the phone screen still follows any pasted string, including `javascript:`**). **D31** filed.

### Money (D24)
No screen here returns a price, rate or wage to a role that may not see it. D10/D11/D13 select none (each has a walk-the-keys test over every role's payload); D12 is Owner-only end to end and is tested for all eight roles at the read, the write, the page and the audit payload. **F-15 was not touched and is not decided — it waits on Arjun.**

### A real bug found on the way
The D13 add form's **select lost the person's choice after a failed add** (React resets a form after an action and a select's default does not survive it). The same bug was in **D10's edit panel**, which had already passed: a new D10 test failed, the select is now keyed on the handed-back value, and both are covered.

### One existing guard was edited, on purpose
`audit-payloads.test.ts` reads every `logAuditEvent` call site and holds an exact registry. `createRuleRevision`'s action is now `opts.action ?? 'UPDATE_RULE'` (so a scheduled change audits as `SCHEDULE_RULE`), so its registry entry was **renamed** to that text, with a comment saying which tests prove a wage figure never reaches the row. Nothing was loosened.

### Verification, honestly
`tsc --noEmit --skipLibCheck` prints **nothing**; `pnpm vitest run` **163 files, 3740 passed + 10 expected-fail** (Part D ended at 145 / 3284 / 10: **+18 files, +456 tests**); `pnpm build` passes (`/mis/settings/rules` is new; `/mis/documents`, `/mis/traceability`, the masters and customers pages are rewired). ESLint is clean on every new file; the one error in a touched file is a pre-existing `as any[]` in `documents/page.tsx`. **Mutation testing:** D12 **79** mutations and D13 **71** (gates, seam, validators, audit shape, href safety, handback, layout classes) — every survivor was either strengthened by a new test or is a documented equivalent; D10 and D11 were mutation-tested the same way (their survivors were strengthened too). The checkers found, and the build fixed: D12 per-key validation, the audit "before", a URL-borne alert, a circular phone link; D13 a control character in a site path; D10 the select reset.
**Not verified:** nothing seen in a browser at any width; no screenshot comparison; the three new desktop forms were exercised only in jsdom.

### Pending — the next agent must do this first
1. **Three dashboard widgets still show empty states** (`wastage.byPhase`, `qc.aqlThisMonth`, `qc.defectsOpen`): `getWastageReport`, `getQcHourlyGrid` and `getDefectReport` now exist — wiring them is small.
2. **A real-browser pass** at 1440, 1200 and 900px over D4–D14, plus drag-and-drop (mouse-wired, keyboard-tested only).
3. **Phase 25 must read F-22 first**: an as-of reader for rules (so a re-judgement of past work uses that day's threshold), and it must not treat D12's history as proof that other screens read as-of.
4. **Human — decisions for Arjun:** **F-15** (may the person who raises a PO also price it?), **D30** (should the old settings list also need a start day and a reason?), **D31** (documents: store the files? versioned job cards/COAs?), F-19 (a unit + disposition on a defect), F-21 (an issue → receipt link), F-16 (wastage allowance), F-17 (weekly off), F-14 (downtime table), D29 (a store dashboard). **Schema items collected, none written:** Hindi name on machines/items (F-20), rule units (F-22), document category/version/retention/batch (F-23), issue → receipt link (F-21).

## Files to attach to the next phase
`DEVELOPMENT_GUIDE.md`, `MIS_UI_SPEC.md`, `DECISIONS.md`, `PHASE_LOG.md`, `phase-reports/phase-24.md`, `qa/FINDINGS.md`

## Part D (2026-09-22) — F-06 closed, then D7 reports, D8 attendance month, D9 QC hourly grid, D14 defects & rework

### F-06 first — closed before D7 was built
D7 builds on `getStoreReport`, so the leak was closed first, in application code, with per-role tests at the server function (D24). One helper carries it — `lib/mis/money-fields.ts` (`withoutMoneyFields`, `forRole`): the two material-price keys (`ratePerUnit`, `pricePerUnit`) are REMOVED (not blanked) at any depth unless the caller holds `wages.read`. Applied to `getStoreReport` (not selected AND stripped from `raw`), `getPO`, `getGRN` (a GRN carries its PO's lines — found by the sweep), `listItems` / `getItem` / `searchItems`, and the return value of every WRITE function (`addPOItem`, `updatePOItem`, `removePOItem`, `createItem`, `updateItem`, `deleteItem`, `restoreItem`, `addBomMaterial` — the second checker pass found `updateItem(id, {})` would have read a stock price back). `computePoTotal` IS money, so it is now `wages.read`; the PO, supplier and print pages ask only for the Owner. **The BOM audit payload:** the audit writer's redaction list now includes the two keys, so a price is stored `[redacted]` (the Owner loses old/new price history — the trade D24 already made for wages). The four `getStoreReport` `it.fails` markers became ordinary tests; `material-money.test.ts` and `store-money.test.ts` (which finally tests `store.ts`'s `=== 'OWNER'` gates) hold the rest, all eight roles each. Two checker passes: FAIL → fixed → PASS. **New F-15:** writing a price is not gated (a PO raiser types a rate they cannot read back) — a policy question, not decided here.

### The four screens (each: build → mutation-test → checker → fix → re-check)
- **D7 · `/mis/reports`** (wastage tab) — `getWastageReport` + pure `lib/mis/wastage.ts`; weeks in the factory zone (D22), **one unit at a time** (never Kg + Nos), phase hues ranked over the whole window so a machine filter never repaints a phase, four named series then one neutral "other" (never cycled), a percentage that always names its denominator. **The CSV export is the same `WastageReport` laid out as rows** (`/api/mis/reports/wastage`, thin route, CSV-injection guard). Verdicts: FAIL (array-valued params, phone screen exposed to a desktop failure, an untested west-of-UTC window) → fixed → PASS.
- **D8 · `/mis/attendance`** — `getAttendanceMonthView`; **the footer is the sum of the visible cells** and the identity `slots = workers × days = present + half + absent + leave + not-recorded` is checked in code and asserted by property tests. "Not recorded" is its own dashed state — never an absence. Overtime is a stripe (one cell per day). The day panel shows the working in exact minutes and says out loud when recorded overtime disagrees with what the punches imply (**the artboard's own example rounds 11 h 58 min to "12.0 h" before subtracting**). Any explicit `?view` is the register at every width. FAIL (register tabs bounced back to D8; a dead-end failure state; copy) → fixed → PASS.
- **D9 · `/mis/qc/grid`** — `getQcHourlyGrid`; columns come from the shift definition, hours from the factory clock; **six states, never collapsed**: passed, failed, make-ready (grey, recorded N/A), **not taken (dashed — nothing recorded)**, not due yet, and not running (a line's hours outside its booking). A failure carries its clearance (a later PASS on the same order/stage/parameter). **AQL limits are `aql.read` (D6): the `limits` key is absent for ADMIN/SUPERVISOR/QC** — they get the verdict and the sample only. PASS with one MEDIUM (hours before a line was running read as "never taken") → fixed with the `off` state → PASS.
- **D14 · `/mis/qc/defects`** — `getDefectReport`; sorted Pareto with the **80% cut drawn as a row and a sentence**, a three-step drill (reason → machine → shift), a log of FAIL checks. **Severity is only the master's** (a check's defect type is free text; only an exact match to a master is classified, the rest says "not classified"). The machine is derived from the booking covering the check (one / several / none — never a guess). **No money anywhere:** the artboard's "Cost of poor quality ₹1,18,400" has no source (no rate, scrap, rework or disposition record exists), so a dashed "Not recorded yet" card says so; nothing is computed and nothing is in the payload for any role. FAIL (six findings: filter-blind copy, a lying empty state, a new hex, unstated schema facts, a client-side percentage, an unlabeled table) → fixed → PASS.

### Honest gaps — each logged in `qa/FINDINGS.md`
F-16 (D7: no wastage allowance, no event log, one unit per log row, no utilisation report) · F-17 (D8: no weekly-off/working-day rule; approved leave writes no attendance row; the "hourly rate" line is pay and D26 replaced its formula; no payroll export exists; pool is a department filter, not D4's people scope) · F-18 (D9: a QC check has no machine; the phone board fixes hours 09–16 with the server clock; D4's `hourlyQcStrip` counts N/A as PASS; run windows come only from bookings) · F-19 (D14: no defect log, free-text defect type, no unit on the defect quantity, no disposition/cost/rework time; the AQL defect lines live only in the audit snapshot).

### Verification, honestly
`tsc` silent; `pnpm vitest run` **145 files, 3284 passed + 10 expected-fail** (Part C ended at 128 / 2768 / 14: **+17 files, +516 tests**, and the 4 F-06 `getStoreReport` markers became ordinary tests); `pnpm build` passes with `/mis/qc/defects`, `/api/mis/reports/wastage` and the three rewired pages; eslint clean on every new file (three `any`s remain in `qc/grid/page.tsx` and one in `reports/page.tsx` — pre-existing, not touched). **~130 mutations** (in place and restored) across F-06 (F1–F11, S1–S8), D7 (W, R, C, X), D8 (A, B, C), D9 (Q, R, X, N) and D14 (D, E, F, G) — every one turned named cases red, after strengthening the ones that survived (D7 X1 label-every-week, D8 B7 unapproved leave and C11 recorded overtime, D9 R6/R7 and the west-of-UTC window, D14 G8 severity bar).
**Not verified:** nothing seen in a browser at any width; no screenshot comparison; drag-and-drop still untested.

### Pending — the next agent must do this first
1. **24.4 (sonnet), four screens left:** D10 master data, D11 traceability, D12 business rules, D13 documents. Follow the pattern in the Phase 24 stamp.
2. **Three dashboard widgets still show empty states** (`wastage.byPhase`, `qc.aqlThisMonth`, `qc.defectsOpen`): their queries now exist (`getWastageReport`, `getQcHourlyGrid`, `getDefectReport`) — wiring them is small and was not part of this brief.
3. **Human — decisions to put to Arjun:** F-15 (may the person who raises a PO also price it?), F-19 (a unit on a defect quantity + a disposition — the two changes that unlock a defect rate and "what was decided"), a wastage allowance per machine/process (F-16), a weekly-off rule (F-17).

## Files to attach to the next phase
`DEVELOPMENT_GUIDE.md`, `MIS_UI_SPEC.md`, `DECISIONS.md`, `PHASE_LOG.md`, `phase-reports/phase-24.md`, `qa/FINDINGS.md`

## Part C (2026-09-22) — D4 order detail, D5 machine timeline, D6 BOM costing

Three desktop screens, one at a time, each built and then checked (D4 PASS; D5 FAIL → fixed → PASS; D6 FAIL on a new hex, tap targets, table semantics and a second route to the BOM rate → fixed → PASS). **Each is a second view of the phone layer's server functions, never a second data path**, and each collapses to its phone equivalent below 1024px with CSS only (`lg:hidden` / `hidden lg:block`; no other breakpoint anywhere in the desktop trees, asserted).

- **D4 · `/mis/orders/[id]`** — `getOrderDesktopView` composes `getOrder`, `getPhasesForOrder`, `getBom`, `getQcForOrder`, documents, production and the schedule; the phase logic (`lib/mis/order-timeline.ts`: signed / running / reopened / blocked-by-N / not-yet / N-A, hourly QC strip, progress) is pure and tested. **No money — the view type has no rupee field** (asserted by scanning the type). The blocker sits inside the running phase as `role=alert`; one primary action, the sign-off for the phase actually running, and only for a person who may sign. `?view=classic` keeps the full phone screen (production/QC logs, reopen) reachable on a desktop.
- **D5 · `/mis/machine-board`** — `getMachineDayTimeline` (production.read) draws one shift on one axis (`lib/mis/machine-timeline.ts`), night shifts included, all time in the factory zone (D22). Free time is a gap, not a colour; booked / running / past-booking / down look different; down is hatched. A Grid | Day-timeline toggle exists only when the shift and data exist.
- **D6 · `/mis/bom/[orderId]`** — one tree; **the Owner sees an extra column, not a different screen**. `getBomCosting` is new, gated `wages.read`, integer-paise arithmetic (`lib/mis/bom-costing.ts`), returns formatted strings. **A line with no rate says "not priced" and is never ₹0; a total that leaves one out is labelled a floor.** The Costing toggle is a link that reloads the page without the column — it drops it, it does not hide it.

### Who may see BOM costing (D24) — checked before building
Owner only. `wages.read` is the wage rule (BRD S9 / D24; not D6, which is the AQL rule). Enforced twice, independently: `getBomDesktopView` never calls `getBomCosting` for a role without `wages.read` (asserted with a spy — `bom-desktop-costing-call.test.ts`), and `getBomCosting` refuses again. **All seven non-Owner roles are refused at the function with zero queries** (`bom-desktop.test.ts`); for ADMIN / SUPERVISOR / QC the payload is scanned for money words and has none — keys are **absent**, not null. **F-06's BOM half is closed**: `getBom` now removes `ratePerUnit` for anyone without `wages.read`, so the phone screen's `isOwner &&` no longer stands alone (`wage-leak.test.ts`, 3 `it.fails` → real tests). The D6 checker found a second route round it — `getOrderTrace` did its own BOM query — now closed through one shared `withoutRates` (`traceability-money.test.ts`, 8 roles). Any future server function returning a BOM tree must use it.

### Honest gaps (a figure with no recorded source is left out, not invented)
- **D4:** batch/lot, customer PO, template, "raised by", AQL level, issued quantity, order-scoped attach. Orders carry no quantity (D13).
- **D5:** downtime reason / who / since, "Mark back in service", the clear chip — nothing records them and no function switches a machine back on (**F-14**). The panel says so and offers no action.
- **D6:** version list and approvers, "Clone from existing", per-unit and rolled-up-to-N quantities and per-unit cost (no order quantity, D13), "Add node" inline (replaced by "Edit structure" → `?view=edit`, the existing phone editor). The Versions card says versions are not kept.

### Verification, honestly
`tsc` silent; `pnpm vitest run` **128 files, 2768 passed + 14 expected-fail** (Part B: 116 / 2430 / 17 — +12 files, +338 tests, and the 3 F-06 BOM markers became ordinary tests); `pnpm build` passes with the three routes; eslint clean on the D6 files. Mutations in a scratch copy or in place-and-restored: D4 blocker/sign-off/truncation, D5 window/bars/utilisation, D6 costing (C1–C7: float multiply, unpriced-as-zero, floor flag…), gates (K1–K6) and the component (M1–M8: column on `canCost`, ₹0 for null, no floor label, overlay for everyone, toggle href, stage note, toggle/edit for everyone) — every one turned named cases red **after** fixes. Three survivors were real and are now tested: C3 (half-paisa cases), D5's gate removal (masked behaviourally — caught by `server-gates.test.ts`), **K2** (the view forgetting its own `canCost` was masked by `getBomCosting`'s gate; pinned by the spy test).
**Not verified:** nothing seen in a browser at any width; no screenshot comparison; the phone BOM screen still receives Prisma `Decimal` quantities as before (not changed, not browser-tested).

### Two outside-the-brief changes
`getBom` and `getOrderTrace` (existing functions) now strip the rate for non-Owners — required by "check who may see BOM costing", and it closes F-06 for BOM. `docs/qa/FINDINGS.md` gets **F-14** and a note that F-06 is half-open: `getStoreReport`, PO item rates, `MisItem.pricePerUnit` and the BOM **write** path (`ADD_BOM_MATERIAL` audits `after: rec`, whose `ratePerUnit` key `redact()` does not know) remain.

## Part B (2026-09-22) — persistence, Customise, the Owner dashboard

**The gate was run**: `mis_dashboard_widgets` is applied and the client regenerated. Everything below is built on it.

- **Persistence** — `server/mis/dashboard.ts`: `getDashboardLayout` reads the rows and re-checks every one against the role on the way OUT; `saveDashboardLayout` replaces the layout in ONE transaction; `resetDashboardLayout` deletes this person's rows. No rows means "never customised" → the role default. **Clearing every widget is therefore the same act as Reset** — there is no row that means "deliberately empty", and D2 keeps Reset always available anyway. Not audited, on the same reasoning as `setLocale`: where someone puts their own widgets is furniture, and auditing it would bury real events.
- **The browser sends an ORDER, never geometry.** `flowLayout(keys, role)` derives x/y/w/h on the server, so a posted layout cannot overlap widgets or resize one past the library — and a test asserts the payload has no `x`/`y`/`w`/`h` in it. This is also why reordering needs no collision code (D2).
- **Customise (D2)** — indigo mode bar, add from the library, remove, reorder, Reset, Done. Drag is the mouse path; **arrow buttons are the keyboard one**, because a dashboard only rearrangeable by dragging cannot be rearranged by everyone. A refused save keeps you in the mode and says why.
- **24.3 the Owner dashboard (D1)** at `/mis/dashboard` — 14 widget bodies, four columns from 1024px and one below, charts degrading to sparklines. Only what the layout shows is fetched, which is also how D24 holds at page level: `getMonthWageBill` is called only when the money widget survived the role-filtered layout, so a non-owner's page never asks for a wage.
- **"Open MIS"** — one flag-gated entry in the existing workspace sidebar (`showMisNav`, decided on the server from `isMisEnabled`). **Three files outside a `mis` folder** (`components/shell/sidebar.tsx`, `dashboard-shell.tsx`, `app/(dashboard)/layout.tsx`): the MIS ships dark, so the decision cannot be made in the browser — an unflagged bundle must carry no hint the module exists.
- **Two small server additions, both reusing existing tables:** `getProductionSeries` (one query for 14 days, factory-day bucketed per D22) and `countPhasesForOrders` (one query, so a list of orders is not N+1). The second keeps D1's promise that "Orders in flight" shows phases complete, "not a guessed percentage".

**Honest gaps in 24.3:** the plan line is absent because no plan figure is recorded anywhere — a guessed plan would make the chart lie. `wastage.byPhase`, `qc.aqlThisMonth` and `qc.defectsOpen` show the empty state: the queries behind them belong to D14 and D9 in 24.4.

## The gate — done (2026-09-22)
`MisDashboardWidget` is in `prisma/schema.prisma`; its SQL is parked, unapplied, at `prisma/migrations-pending/20260921000000_mis_dashboard_widgets/`. No TypeScript references it yet, so `tsc` is clean.
```
mv prisma/migrations-pending/20260921000000_mis_dashboard_widgets prisma/migrations/
pnpm db:deploy && pnpm db:generate
```
**Phase 24's header did not say SCHEMA GATE and §2.3's list did not include it** — D2 stores a layout as *rows* (`widgetKey, x, y, w, h` per user), which is a table. Both are corrected, and Phase 25 was added to the list for the same reason (D27's payroll snapshot).

## What was built
- **24.1 · the shell (D3)** — `components/mis/desktop/desktop-shell.tsx` + `DesktopPageHeader`. `MisShell` now renders the desktop frame **and** the phone frame in one tree and lets CSS pick (`hidden lg:flex` / `lg:hidden`). Dark 68px rail with hover labels carrying the full word and its count, widening to 240px at 1280. Sections from `lib/mis/desktop-nav.ts`, grouping only what `getNavigationFor` already permitted — nothing widened, nothing greyed.
- **24.2 · the library (D2)** — `lib/mis/widgets.ts`: 14 widgets, grid sizes, the permission each needs, `sanitiseLayout` (drops a retired key, drops a widget the role may not hold, corrects sizes, refuses grid overflow) and `defaultLayoutFor`. `server/mis/dashboard.ts`: the catalogue, the layout, and `assertMayPlaceWidgets`.
- **Charts** — `lib/mis/chart.ts`: one scale/one axis, D1's five computed hues, sparkline degradation. Green/amber/red reserved for state.

## Decisions
Cited D2, D6, D16, D24, D25, D27, D28. New: **D29** (a role with no widgets — STORE_GUY — gets the honest empty state; no Store group was invented, and no permission was widened to light one up).

## The money rule (D24)
Read and write refuse differently, on purpose. **Reading omits:** a non-owner's catalogue has no MONEY group and a layout read drops a money row, so a demotion cannot leave a wage on a saved dashboard — an error page is the wrong answer to "show me my dashboard". **Writing throws:** `MisForbiddenError`, refusing the whole layout rather than saving the allowed part. Both asserted for all seven non-Owner roles at the server function. `dashboard.ts` is mocked with a Proxy that throws on any database access, so it cannot become a second data path.

## Verification, honestly
**Part B:** `tsc` silent; `pnpm vitest run` **116 files, 2430 passed + 17 expected-fail**; `pnpm build` passes with `/mis/dashboard`; eslint clean on every touched file. Seven more mutations in a scratch copy — read stops re-checking the role, save skips the permission check, save appends instead of replacing, reset deletes everyone's rows, remove does nothing, reorder is a no-op, the library ignores the server catalogue — each turned named cases red. Two pinned tests fired as designed and were updated deliberately, not worked around: `server-gates.test.ts` (the three layout writers are session doors) and `wage-screens.test.tsx` (the dashboard page is now a registered importer of a wage module).
**Part B not verified:** still nothing seen in a browser at any width; drag-and-drop is untested (the keyboard path is what the tests drive); no screenshot comparison; the migration has not been exercised against real Postgres beyond `db:deploy` succeeding.

**Part A:** `tsc` silent; `pnpm vitest run` **115 files, 2381 passed + 17 expected-fail** (14F left 110 / 2204, so +5 files and +177 tests — exactly the five new files: widgets 66, dashboard 56, chart 29, desktop-shell 17, desktop-nav 9); `pnpm build` passes; eslint clean on every touched file. Twelve mutations run in a scratch copy: catalogue unfiltered, money widget re-permissioned, role re-check removed from the read, retired key kept, grid overflow allowed, write-path assert deleted, empty group kept, frame breakpoint moved to `md`, rail target shrunk, 1280 widen removed, empty sections kept, rail greyed instead of omitted — every one turned named cases red.
**Not verified:** nothing has been seen in a browser at any width; no screenshot comparison against the artboards; the parked SQL has not run against Postgres; no widget renders real data yet.

## Two things changed that were not asked for
- **The phone layout now takes over below 1024px, not 768px.** D3 says so. At 768–1023px the old code showed a light left rail belonging to neither layout. The now-unreachable `SideNav` was deleted rather than left hidden.
- **`axisTicks` uses a 1 / 1.2 / 2 / 2.5 / 5 family.** A plain 1/2/5 cannot produce D1's own axis (0–48k in 12k steps for a 40,850 reading); it jumps to 20k and wastes a third of the height.

## Pending — the next agent must do this first
1. **24.4 (sonnet):** the eight remaining D screens, each against its artboard — D7 reports, D8 attendance month, D9 QC hourly grid, D10 master data, D11 traceability, D12 business rules, D13 documents, D14 defects/rework (D4–D6 are done, Part C). **Several sessions, not one.** Use `DesktopPageHeader` (one filled button per page) and the chart primitives; D9 and D14 also supply the queries three dashboard widgets are waiting on.
2. **Drag-and-drop** is wired for the mouse but has no test; the keyboard path is what the suite drives. Worth a real-browser pass.
3. **Human:** decide F-14 (a downtime table + who may switch a machine back on) and whether BOM versions are wanted (D6 draws them; nothing stores them); D29 to Arjun (does the store manager get a dashboard?); look at the shell and the dashboard in a real browser at 1440, 1200 and 900px; confirm the "Factory MIS" sidebar entry appears only for a flagged account.

## Files to attach to the next phase
`DEVELOPMENT_GUIDE.md`, `MIS_UI_SPEC.md`, `DECISIONS.md`, `PHASE_LOG.md`, `phase-reports/phase-24.md`, `qa/FINDINGS.md`
